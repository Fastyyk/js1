

let number = (4, 7, 2, 8, 4, 5,);
let sun = 0;

for (let 1 = 0; 1 < number.length; 1++) (
    sun += number[1];
)
let average = sun / number length;
console.log("Середнє значення масиву", average )

let max = number[0];
for (let i = 1; <number.length i++) {
    if (number[1] > max) {
        max = number[i]
    }
}
console.log("Елемент з найбільшим числом")